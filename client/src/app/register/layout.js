export const metadata = {
  title: 'Create Account',
  description: 'Join CrimeWatch — register to report crimes anonymously, track incidents, and contribute to community safety.',
};

export default function RegisterLayout({ children }) {
  return children;
}
