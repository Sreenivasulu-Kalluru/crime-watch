export const metadata = {
  title: 'Dashboard',
  description: 'Authority dashboard — view crime statistics, manage reports, and track incident resolution progress.',
  robots: { index: false, follow: false },
};

export default function DashboardLayout({ children }) {
  return children;
}
