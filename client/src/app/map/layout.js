export const metadata = {
  title: 'Live Crime Map',
  description: 'View real-time crime incidents on an interactive map. Filter by category and stay informed about safety in your area.',
};

export default function MapLayout({ children }) {
  return children;
}
