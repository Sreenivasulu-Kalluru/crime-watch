export const metadata = {
  title: 'Report a Crime',
  description: 'Submit a crime report with geolocation, photos, and videos. Report anonymously to protect your identity while helping the community.',
};

export default function ReportLayout({ children }) {
  return children;
}
