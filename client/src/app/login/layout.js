export const metadata = {
  title: 'Sign In',
  description: 'Sign in to your CrimeWatch account to report crimes, track incidents, and help keep your community safe.',
};

export default function LoginLayout({ children }) {
  return children;
}
