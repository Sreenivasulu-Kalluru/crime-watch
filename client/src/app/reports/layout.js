export const metadata = {
  title: 'Crime Reports',
  description: 'Browse all reported crime incidents. Filter by category, severity, and status to find relevant reports.',
};

export default function ReportsLayout({ children }) {
  return children;
}
